image : microsoft/dotnet:latest
stages:
  - build
before_script:
  - 'dotnet restore'
build:
 stage: build
 script:
  - 'dotnet build -c Release'
 artefacts:
  paths:
  - sly/bin/Release/netcoreapp1.0/*.*
 only:
   - master