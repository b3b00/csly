namespace sly.lexer
{
    public enum GenericToken
    {
        Default,
        Identifier,
        Int,
        Double,
        Date,
        KeyWord,
        String,
        Char,
        SugarToken,

        Extension,

        Comment,
        UpTo
    }
}